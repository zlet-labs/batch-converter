# Recommended GitHub repository metadata

Use these values in the GitHub **About** panel when repository settings are updated.

## Description

```text
Local batch file converter for Windows with safe previews, preserved originals, and no cloud uploads.
```

## Website

Leave empty until a real Zlet Labs product page exists and matches the released application.

## Topics

```text
batch-converter
file-converter
windows
dotnet
wpf
offline-first
privacy
json
markdown
portable-app
```

Add format-specific topics such as `docx`, `xlsx`, or `pptx` only after those conversions are implemented and released.

## Social preview

Use a real Zlet Labs banner from `docs/images/` after the current UI is finalized. Do not upload a mock interface or advertise unsupported conversions.

## Releases

Public downloadable binaries belong on:

https://github.com/zlet-labs/folder-converter/releases

Do not treat automatically generated source archives as application downloads.
